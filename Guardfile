require './lib/helpers'

# SASS
guard 'sass', :input => 'source/sass', :output => 'public/stylesheets'


# HAML
guard 'haml', :input => 'source', :output => 'public' do
  watch(/^.+(\.html\.haml)/)
end

